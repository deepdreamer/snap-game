services:
  app-dev:
    user: "${USER_ID}:${GROUP_ID}"
    image: node:24-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"